services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: briefbot
      POSTGRES_USER: briefbot
      POSTGRES_PASSWORD: briefbot
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "127.0.0.1:5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U briefbot"]
      interval: 5s
      timeout: 3s
      retries: 10
  opencode:
    image: ghcr.io/anomalyco/opencode:1.18.17
    command: ["serve", "--hostname", "0.0.0.0", "--port", "4096"]
    working_dir: /workspace
    environment:
      OPENCODE_SERVER_USERNAME: ${OPENCODE_SERVER_USERNAME:-opencode}
      OPENCODE_SERVER_PASSWORD: ${OPENCODE_SERVER_PASSWORD}
      OPENCODE_API_KEY: ${OPENCODE_API_KEY}
      HOME: /tmp/opencode
    volumes:
      - ./.opencode:/workspace/.opencode:ro
      - ./opencode.json:/workspace/opencode.json:ro
    ports:
      - "127.0.0.1:4096:4096"
    user: "1000:1000"
    tmpfs:
      - /tmp
    restart: unless-stopped
  stt:
    image: fedirz/faster-whisper-server:latest-cpu
    environment:
      WHISPER_MODEL: ${STT_MODEL:-Systran/faster-whisper-small}
      WHISPER_COMPUTE_TYPE: int8
    volumes:
      - whisper_cache:/root/.cache/huggingface
    ports:
      - "127.0.0.1:8000:8000"
    restart: unless-stopped
  app:
    build: .
    env_file:
      - path: .env
        required: false
    environment:
      DATABASE_URL: postgres://briefbot:briefbot@postgres:5432/briefbot
      OPENCODE_BASE_URL: http://opencode:4096
      AUDIO_STORAGE_DIR: /var/lib/briefbot/audio
    depends_on:
      postgres:
        condition: service_healthy
      opencode:
        condition: service_started
      stt:
        condition: service_started
    volumes:
      - audio_data:/var/lib/briefbot
    ports:
      - "127.0.0.1:3000:3000"
    restart: unless-stopped
volumes:
  postgres_data:
  whisper_cache:
  audio_data:
